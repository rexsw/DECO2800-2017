package com.deco2800.marswars;

import com.deco2800.marswars.entities.BaseEntity;
import com.deco2800.marswars.worlds.InitialWorld;

/**
 * Tests for the InitialWorld class.
 *
 * @author @luanlucho
 *
 * @see InitialWorld
 *
 */
public class InitialWorldTest {

    // An initial world mock object
    InitialWorld initialWorld;
    // An entity mock object
    BaseEntity entity;

//    @Before
//    public void setup(){
////////////// !!!!!!!!!!! fails to instantiate world dunno why !!!!!!!!!!!!!!!!
//        initialWorld = new InitialWorld();
//        entity = new HeroSpacman(initialWorld,0f,0f,0f);
//    }

    /**
     * Test the initial state of the collision map
     */
//    @Test
//    public void testGetCollisionMap(){
//        Array2D<List<BaseEntity>> collisionMap = initialWorld.getCollisionMap();
//        for(int x = 0; x < collisionMap.getWidth(); x++){
//            // NOTE: length = height
//            for(int y = 0; y < collisionMap.getLength(); y++){
//                Assert.assertTrue("Tile shouldn't contain entities",
//                        collisionMap.get(x,y).isEmpty());
//            }
//        }
//    }
}
